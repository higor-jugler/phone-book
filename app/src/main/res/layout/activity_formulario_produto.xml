<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <EditText
        android:id="@+id/activity_formulario_nome"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:hint="Nome"
        android:inputType="text"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/activity_formulario_descricao"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Descrição"
        android:inputType="text"
        app:layout_constraintEnd_toEndOf="@id/activity_formulario_nome"
        app:layout_constraintStart_toStartOf="@id/activity_formulario_nome"
        app:layout_constraintTop_toBottomOf="@id/activity_formulario_nome" />

    <EditText
        android:id="@+id/activity_formulario_numero"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Número"
        android:inputType="phone"
        app:layout_constraintEnd_toEndOf="@id/activity_formulario_descricao"
        app:layout_constraintStart_toStartOf="@id/activity_formulario_descricao"
        app:layout_constraintTop_toBottomOf="@id/activity_formulario_descricao" />

    <Button
        android:id="@+id/activity_formulario_botao"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:text="Salvar"
        app:layout_constraintVertical_bias="1"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/activity_formulario_numero" />


</androidx.constraintlayout.widget.ConstraintLayout>